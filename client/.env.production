# Production Environment Variables
# For Vercel deployment where frontend and backend are on the same domain

# Empty string means use relative URLs (same domain)
# This allows the app to automatically use the correct domain in production
VITE_API_URL=http://localhost:5000
